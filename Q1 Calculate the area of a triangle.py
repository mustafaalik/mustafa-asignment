# Calculate the area of a triangle
base = float(input("Enter the base of the triangle: "))  # Get the base from the user
height = float(input("Enter the height of the triangle: "))  # Get the height from the user

area = (base * height) / 2  # Calculate the area using the formula

print("The area of the triangle is:", area)  # Print the calculated area
